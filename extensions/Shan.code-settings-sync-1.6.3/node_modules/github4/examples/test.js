"use strict";

var Client = require("./../lib/index");

var github = new Client({
    debug: false,
    includePreview: true
});

github.users.getById({ id: "429706" }, function(err, res) {
    console.log(err, res);
});
